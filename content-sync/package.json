{
  "name": "adfin-content-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "sync:events": "node --env-file=.env src/sync-events.js",
    "sync:events:dry": "node --env-file=.env src/sync-events.js --dry-run"
  }
}
